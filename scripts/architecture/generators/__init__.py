"""Generators: turn scanner facts into `.ai/` artifacts.

A generator composes facts and attaches provenance. It never reads source
directly and never invents a fact a scanner did not produce — see
scripts/architecture/README.md.
"""
